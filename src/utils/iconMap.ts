import {
  FiCode,
  FiServer,
  FiDatabase,
  FiCloud,
  FiCpu,
  FiLayers
} from "react-icons/fi";

export const iconMap: Record<string, any> = {
  code: FiCode,
  server: FiServer,
  database: FiDatabase,
  cloud: FiCloud,
  cpu: FiCpu,
  layers: FiLayers
};